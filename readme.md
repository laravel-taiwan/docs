# Laravel 文件

您可以在 [https://laravel.com/docs](https://laravel.com/docs) 找到 Laravel 文件的線上版本。

## 貢獻指南

如果您正在提交 **目前穩定版本** 的文件，請將其提交到相應的分支。例如，Laravel 6 的文件應提交到 `6.x` 分支。預計用於下一個 Laravel 版本的文件應提交到 `master` 分支。
