# Laravel 文件

您可以在 [https://laravel.com/docs](https://laravel.com/docs) 找到 Laravel 文件的線上版本。

## 貢獻指南

如果您正在提交**當前穩定版本**的文件，請將其提交到相應的分支。例如，針對 Laravel 12 的文件應提交到 `12.x` 分支。針對下一個 Laravel 發行版的文件應提交到 `master` 分支。
